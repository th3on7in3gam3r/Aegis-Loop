{
  "name": "aegis-loop",
  "private": true,
  "scripts": {
    "build": "npm install --prefix code",
    "start": "npm start --prefix code",
    "test": "npm test --prefix code"
  },
  "engines": {
    "node": ">=20"
  }
}
